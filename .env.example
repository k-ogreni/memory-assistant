OLLAMA_MODEL=llama3.2

# Path to the JSON file used for long-term memory storage
DATA_PATH=data/users.json

# Max conversation turns kept in working memory (controls context window usage)
MAX_HISTORY_TURNS=8
